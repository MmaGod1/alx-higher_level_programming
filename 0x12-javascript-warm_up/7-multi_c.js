#!/usr/bin/node
const x = parseInt(process.argv);
  for (let i = 0; i < x[2]; i++) {
	  if (IsNaN(x[2])) {
		  console.log('Missing number of occurrences');
	  } else {
		  console.log('C is fun');
	  }
  }
